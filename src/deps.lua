-- each entry in this table is the name of a module 
-- if any are not found, a prompt is displayed to install them
return
{
    {
        require_name = "cjson",
        install_name = "lua-cjson",
    },
}
